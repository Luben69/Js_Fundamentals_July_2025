function matrix(n) {
    for (let i = 0; i < n; i ++) {
        let result = [];
        for (let j = 0; j < n; j ++) {
            result.push(n);
        }
        console.log(result.join(' '));
    }
}